{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Steno desktop windows. Allows core APIs, event APIs, opener APIs and borderless-window dragging for the main shell, quicknote (floating capture) window, sticky notes, canvas, Zen, search and settings windows.",
  "windows": ["main", "quicknote", "sticky-*", "canvas", "zen-*", "search", "settings"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-start-dragging",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:event:default",
    "opener:default"
  ]
}
