{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability covering the Steno main window and the quicknote floating window. Allows core APIs (window show/hide/focus, app exit, IPC to custom Rust commands). Plugin-side permissions for global-shortcut are not needed here because PR1/PR2 register shortcuts from Rust only; future PRs that expose registration to JS should add `global-shortcut:default`.",
  "windows": ["main", "quicknote"],
  "permissions": ["core:default"]
}
