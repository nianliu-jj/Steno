## 1. 设置面板结构

- [x] 1.1 将 `SettingsView.vue` 重构为可嵌入的设置面板组件，支持关闭事件并保留独立设置窗口兜底展示
- [x] 1.2 实现顶部分类标签、滚动内容区和底部操作栏，分类包含常规、外观、快捷键、隐私安全、存储、关于
- [x] 1.3 将现有主题、快捷键、速记浮窗、编辑器、备份和路径展示逻辑迁移到新的分类结构中
- [x] 1.4 为尚未持久化的隐私安全、强调色、便签纸、应用排除名单等项目增加只读、禁用或规划中状态

## 2. 主窗口模态框入口

- [x] 2.1 在 `MainView.vue` 中添加本地设置模态框可见性状态
- [x] 2.2 使用 Naive UI `NModal` 包裹 `SettingsView`，点击主窗口“设置”入口时打开模态框
- [x] 2.3 确认、关闭和遮罩关闭后隐藏模态框，并保持主窗口当前内容状态
- [x] 2.4 保留 `ui.mode === 'settings'` 的独立设置视图兜底，不破坏直接设置窗口路径

## 3. 视觉与可访问性

- [x] 3.1 按原型调整设置模态框宽高、边框、分组、密度、按钮和路径样式
- [x] 3.2 确保模态框在小窗口下只有内容区滚动，头部标签和底部操作栏保持可见
- [x] 3.3 为图标按钮、分类标签和只读状态补充可理解的文本或 ARIA 标签
- [x] 3.4 检查浅色和深色主题下文本、边框、禁用态和遮罩对比度

## 4. 测试与验证

- [x] 4.1 更新 `SettingsView.test.ts`，覆盖分类切换、路径展示和无 highlight.js 警告
- [x] 4.2 更新或补充 `MainView` 测试，覆盖点击设置入口后显示模态框
- [x] 4.3 运行设置视图和主窗口相关单元测试
- [x] 4.4 运行前端类型检查，确认 Vue 模板类型和事件签名通过
