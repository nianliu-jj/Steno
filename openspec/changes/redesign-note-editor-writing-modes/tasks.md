## 1. 主编辑器核心

- [x] 1.1 为主编辑页与 Zen 页新增独立的 Markdown-first 富编辑器抽象层，并保留现有 `MarkdownEditor.vue` 继续服务轻量编辑场景
- [x] 1.2 实现三态编辑模型（`rich-edit`、`rich-readonly`、`source-edit`）及其切换动作，确保从只读态进入代码态时自动退出只读
- [ ] 1.3 接通富编辑态与代码态之间的 Markdown 同步、自动保存、标签抽取和导出链路

## 2. 主编辑页体验重做

- [x] 2.1 用新主编辑器替换 `NoteEditorView` 的正文区域，并调整顶部卡片圆角、上移和标题留白
- [x] 2.2 在主编辑页底部状态栏增加只读/编辑切换、代码模式切换和进入 Zen 的入口
- [x] 2.3 为普通编辑页增加悬浮大纲入口、树状标题列表和大纲跳转能力

## 3. Zen 与大纲侧栏

- [x] 3.1 用共享主编辑器内核替换 `ZenMode` 的正文区域，并保持 Zen 默认进入排版编辑态
- [x] 3.2 让 Zen 页仅显示大纲侧栏，并在关闭 Zen 时返回进入前页面
- [x] 3.3 为普通编辑页与 Zen 页分别实现大纲侧栏宽度拖拽、折叠阈值、重新展开和跨重启持久化恢复

## 4. 设置与验证

- [x] 4.1 扩展前后端设置存储，加入普通编辑页与 Zen 页各自的大纲宽度和展开状态默认值
- [ ] 4.2 补充模式切换、只读转代码、Zen 返回来源和大纲跳转/折叠/持久化的测试
- [ ] 4.3 回归验证主编辑页视觉布局、Zen 仅大纲侧栏约束，以及 quicknote / sticky 等轻量编辑场景未被本次重构破坏
