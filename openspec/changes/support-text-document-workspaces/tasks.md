## 1. 条目模型与存储分流

- [x] 1.1 为工作区、文件夹、分组、文档、文本建立统一条目索引模型，并扩展前后端类型定义
- [x] 1.2 让 `text` 正文继续存数据库，并在保存链路中按正文 Markdown UTF-8 字节数执行 `10KB` 限制与错误提示
- [x] 1.3 让 `document` 正文落盘到工作区 Markdown 文件，并在数据库中保存文档路径、标题、标签和更新时间等索引信息
- [x] 1.4 为系统默认分组提供初始化逻辑，并让未显式选组的速记文本默认进入该分组

## 2. 工作区、文件夹与分组浏览

- [ ] 2.1 引入工作区、文件夹和分组的层级关系与浏览上下文状态，支持递归进入文件夹和子分组
- [x] 2.2 实现主页面“当前工作区/文件夹内容 + 全局分组/文本”的混合内容视图
- [x] 2.3 新增只展示当前工作区树的右侧结构栏，以及底部栏中的当前工作区显示与切换入口

## 3. 文档落点、转换与列表交互

- [x] 3.1 让主窗口“新建笔记”按当前工作区根目录或当前子文件夹决定文档落点，并在没有工作区时先提示选择工作区
- [x] 3.2 在主列表右键菜单中新增“转为文档”，只对 `text` 生效，并按当前工作区/文件夹执行原地转换
- [x] 3.3 为文件夹、分组、文档、文本卡片增加右上角类型标识，并接通不同类型的点击与右键行为
- [ ] 3.4 在筛选下拉中新增并持久化“文件夹、分组、文档、文本”四类类型筛选
- [x] 3.5 在底部栏中显示仅针对当前页面内容的文档数、文本数、文件夹数和分组数统计

## 4. 验证与迁移

- [x] 4.1 补充文本 `10KB` 限制、默认分组、文档落点、无工作区提示和原地转文档的前后端测试
- [ ] 4.2 补充混合列表、右侧工作区树、卡片类型标识、类型筛选持久化和底部统计的前端测试
- [ ] 4.3 设计并验证从现有单一 SQLite 笔记模型迁移到新条目索引模型的兼容方案，确保现有 quicknote、主编辑页和 Zen 路由不回归
