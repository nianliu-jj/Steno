/**
 * @file Shiki 代码高亮单例与同步高亮 API。
 *
 * Phase 3 实施：lazy `createHighlighter()` + 双主题（github-light / github-dark）+ 同步 `highlightCode`。
 */

export function highlightCode(code: string, _lang: string): string {
  // Phase 3 替换为 Shiki 实际输出
  return code;
}
