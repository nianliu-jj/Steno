/**
 * @file Tauri 相对图片路径转换。
 *
 * Phase 5 实施：复刻 PureMark 的 imagePathPlugin，按 noteDir 拼接绝对路径后
 * 通过 `convertFileSrc` 输出 asset 协议 URL。
 */

export function resolveImageSrc(src: string, _noteDir?: string): string {
  // Phase 5 实施
  return src;
}
